{
  "extends": [
    "config:base"
  ],
  "enabledManagers": ["npm"],
  "packageRules": [
    {
      "packageNames": ["aws-cdk"],
      "packagePatterns": ["^@aws-cdk/"],
      "schedule": ["after 9pm on sunday"]
    },
    {
      "packageNames": ["@types/node", "@types/jest", "ts-test"],
      "enabled": false
    }
  ]
}
